export const config: WorldSettings = {
    WIDTH: 500,
    HEIGHT: 500,
    ELEMENT_SIZE: 10,
};

export const cameraProjectionConfig: CameraProjectionSettings = {
    F: 500,
    ALPHA: 1,
    BETA: 1
}